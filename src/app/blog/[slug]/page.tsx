import React from 'react'

const Page = ({ params }: { params: { slug: string } }) => {
  return (
    <div>

    </div>
  )
}

export const generateStaticParams = async () => {

}

export default Page
